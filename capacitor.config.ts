import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-6.2-on-off-labels-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
